let metros = parseInt(prompt('Informe um valor em metros:'));

if (metros) {
    // console.log printa os valores no console
    console.log(`${metros} metro(s) em centimetros é: ${metros * 100}`);
    console.log(`${metros} metro(s) em milimetros é: ${metros * 1000}`);
}